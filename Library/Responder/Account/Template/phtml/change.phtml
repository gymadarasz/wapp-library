<?php $this->restrict(); ?>
<h1>Change password</h1>
<?php include $this->getTemplateFile('messages.phtml') ?>
<form method="POST" action="?q=change">
    <?=$csrf?>
    
    <input type="hidden" name="token" value="<?=$token?>">
    <span class="errors"><?=@implode(', ', @$errors['token'])?></span>
    
    <input type="password" name="password" value="<?=@$params['password']?>" placeholder="Password">
    <span class="errors"><?=@implode(', ', @$errors['password'])?></span>
    
    <input type="password" name="password_retype" value="<?=@$params['password_retype']?>" placeholder="Password re-type">
    <span class="errors"><?=@implode(', ', @$errors['password_retype'])?></span>
    
    <input type="submit" value="Change password">
</form>
